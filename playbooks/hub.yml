---
# Hub = bare WireGuard relay (fresh Debian 12/13, user "wg" with sudo).
- name: Provision WireGuard hub
  hosts: hub
  become: true
  roles:
    - base_hardening
    - wg_hub
    - frr_hub
    - certs_hub
